# secret para criacao de token
api.security.token.secret=${JWT_SECRET:meusegredosecreto}

# Configuracao do DataSource
## jdbc:tipodobanco://localhost:3306/nomedobanco?configsadicionais
spring.datasource.url=jdbc:mysql://localhost:3306/agro?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=12345678
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuracao do Hibernate
spring.jpa.hibernate.ddl-auto=update

# Configuracao da JPA
spring.jpa.show-sql=true